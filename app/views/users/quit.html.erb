<div class="container">
  <% if flash[:notice] %>
    <div class="flash alert alert-danger">
      <%= flash[:notice] %>
    </div>
  <% end %>
  <div class="row">
    </br>
    <h4 class="text-center font-weight-bolder">本当に退会しますか？</h4>
    <h4 class="text-center">退会すると、会員登録情報や</h4>
    <h4 class="text-center">これまでの購入履歴が閲覧できなくなります。</h4>
    <h4 class="text-center">退会する場合は、「退会する」をクリックしてください。</h4>
    </br>
  </div>
  
    <div class="col-md-2">
      <%= link_to "退会しない（トップ画面に戻る）", root_path, class: "btn btn-info" %>
    </div>
  
    <div class="col-md-2">
      <%= link_to "退会する", out_users_path, method: :patch, class: "btn btn-danger" %>
    </div>
  </div>
</div>